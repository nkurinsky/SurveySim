
Note that to remake the configure/install sequence after new files have been
added to the obs/model/templates directories, the following commands should be run:

./update_makefiles.sh
autoreconf
sudo make install

